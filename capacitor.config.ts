import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.bennyshopmobile',
  appName: 'benny-shop-mobile',
  webDir: 'dist'
};

export default config;
